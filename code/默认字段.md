---
source: 默认字段.txt
tags:
  - 数据库
  - SQL
  - 杂项备忘
---

# 默认字段

```sql
SELECT CONCAT('ALTER TABLE ', TABLE_NAME, ' CHANGE ', COLUMN_NAME, ' ', LOWER(COLUMN_NAME), ' ', COLUMN_TYPE, ';') AS 'SQL Query'
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_SCHEMA = 'event_center' AND TABLE_NAME = 'common_user_role';


SET @row_number = 0;
UPDATE your_table
SET new_id = (@row_number := @row_number + 1);

ALTER TABLE `` 
ADD `CREATE_DATE` datetime,
ADD `CREATE_BY` INT (11),
ADD `UPDATE_DATE` datetime,
ADD `UPDATE_BY` INT (11),
ADD `DEL_FLAG` INT (11) DEFAULT 0 NOT NULL

ALTER TABLE `monitor_enterprise_data` AUTO_INCREMENT = 1

@PatrolAreaCheck(tableName = "UAI",fieldName = MyAopUtils.AREA_ID)

^\[((([1-8]\d?)|([1-8]\d))(\.[1-9]\d*)|90|0(\.[1-9]\d*)),((([1-9]\d?)|(1[0-7]\d))(\.\d{1,8})|180|0(\.[1-9]\d*))]


ALTER TABLE `` 
ADD `create_date` datetime comment '创建时间',
ADD `create_by` INT (11) comment '创建用户',
ADD `update_date` datetime comment '更新时间',
ADD `update_by` INT (11) comment '更新用户',
ADD `del_flag` INT (11) DEFAULT 0 NOT NULL comment '删除标识'


ALTER TABLE `monitor_well_info` 
ADD `order_index` INT (11) DEFAULT 0 NOT NULL


UPDATE monitor_well_info 
SET order_index = 3 
WHERE
	well_level = '国控'


ALTER TABLE `base_device` 
ADD `longitude` decimal(9,6) COMMENT '经度',
ADD `latitude` decimal(9,6) COMMENT '纬度'




/safety-api/baseTDanger/listAllDangerInfo 危险源
/safety-api/processStandingBook/listAllFacilitiesProcess  高危工艺
/safety-api/chemicalStandingBook/listAllFacilitiesChemical 危化品
safety-api/risk/listAllRiskArea 风险分区


Could not transfer artifact com.hl:core-all:pom:1.1-SNATHOST from/to maven-default-http-blocker (http://0.0.0.0/): Blocked mirror for repositories: [inner-nexus (http://192.168.0.243:8081/nexus/content/groups/public, default, releases+snapshots)]
```
